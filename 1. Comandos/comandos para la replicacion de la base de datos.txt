// Conectar a la base de datos de origen
use torneo;

// Definir el array de colecciones de origen
var colecciones_origen = ["arbitros", "deportistas"];

// Definir la base de datos de destino
var nombre_basedatos_destino = "prueba2";

// Iterar sobre las colecciones de origen y copiar documentos a la base de datos de destino
colecciones_origen.forEach(function(coleccion) {
    db[coleccion].find().forEach(function(doc) {
        db.getSiblingDB("prueba2")[coleccion].insertOne(doc);
    });
});